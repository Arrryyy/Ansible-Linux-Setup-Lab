server {
    listen 80;
    server_name {{ inventory_hostname }};

    location / {
        proxy_pass http://node2:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
